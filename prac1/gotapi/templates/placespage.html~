<html>
	<head>
		<title>{{ titlehead }}</title>
	</head>
	<body>
		<h1>{{ pagetitle }}</h1>
		<ol>
		{% for i in contentbody %}
		
		<li>
		{{i.name}}
		<input type="button" value="+Detalls" onClick= location.href="/places/{{i.id}}/" />
		{% if user.username %}
		<input type="button" value="Edit" onClick= location.href="/places/{{i.id}}/edit" />
		{% endif %}
		
		</li>
		{% endfor %}
		</ol>
<input type="button" value="Torna a l'índex" onClick= location.href="/" />

	{% if user.username %}
		<input type="button" value="Crea un Lloc" onClick= location.href="/places/create/" />
	{% else %}
		<p>No estas loguejat:
		Obligatori fer <a href="/login/">sign in</a>
		per modificar llocs.</p>
	{% endif %}
	</body>
<html>
